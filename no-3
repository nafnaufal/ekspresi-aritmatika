#include <iostream>
#include <stack>
#include <ctype.h>
using namespace std;
long unsigned int i;

bool isOp(char x){
	if(x=='+'||x=='-'||x=='*'||x=='/'||x=='%')
		return true;
	return false;
}
int precedence(char x){
	if(x=='+'||x=='-') return 1;
	if(x=='*'||x=='/'||x=='%') return 2;
	return 0;
}
double applyOp(double num1, double num2, char op){
    switch(op){
        case '+': return num1 + num2;
        case '-': return num1 - num2;
        case '*': return num1 * num2;
        case '/': return num1 / num2;
        }
    return 0;
    
    int main(){
    }
